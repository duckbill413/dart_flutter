void main() {
  // 관습적으로 함수나 메소드 내부에 지역 변수를 선언할 때에는 var을 사용
  // 같은 타입으로만 업데이트 가능하다.
  var name1 = '파도기';
  print(name1);
  // class에서 변수나 property를 선언할 때에는 타입을 지정
  String name2 = '파덕파덕';
  print(name2);
}
